version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: npm install
      # - run: npm run lint
      - run: npm run build:prod
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
